# Build output from the Makefile and CMake
build/
build-cmake/

# Fetched benchmark-only comparison headers (moodycamel) - not committed;
# `make bench-compare` fetches them. The library itself is dependency-free.
benchmarks/third_party/

# Xcode user state
xcuserdata/
*.xcuserstate

# Non-shipping experiments and parked WIP. Kept out of the repo rather than
# deleted: these A/B harnesses are the evidence behind published numbers.
# See scratch/README.md. Anything here that starts backing a claim in the
# README should be promoted to a real benchmarks/ target so CI keeps it honest.
scratch/
